DB_USER=watchdog
DB_PASS=watchdog
DB_NAME=datawatchdog
DB_HOST=localhost
DB_PORT=5432

# Notifications - fill in to enable each channel
SLACK_WEBHOOK_URL=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
ALERT_EMAIL=

# Flask - generate one: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=